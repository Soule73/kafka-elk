package com.learn.kafka;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class KafkaApplicationTests {

    /**
     * Vérifie que le contexte Spring Boot se charge correctement.
     * Ce test garantit que la configuration de l'application est valide
     * et que tous les beans nécessaires peuvent être initialisés sans erreur.
     */
    @Test
    void contextLoads() {
        // Si le contexte ne se charge pas correctement, ce test échouera.
    }
}
